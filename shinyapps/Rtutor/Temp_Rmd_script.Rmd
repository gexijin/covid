

## Your request 1
Generate R code, not R Markdown.  Use the df data frame.  Plot SL vs. SW. Color by Species. Use ggplot2.

## R script returned by OpenAI 1
```{R}
df <- user_data()
ggplot(df, aes(x=SL, y=SW, color=Species)) +
  geom_point()
```

